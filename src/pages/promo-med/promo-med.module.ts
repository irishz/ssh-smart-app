import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { PromoMedPage } from './promo-med';

@NgModule({
  declarations: [
    PromoMedPage,
  ],
  imports: [
    IonicPageModule.forChild(PromoMedPage),
  ],
})
export class PromoMedPageModule {}
